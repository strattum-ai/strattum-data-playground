# Deps para os experimentos de ingestão (Postgres → Delta)
# Notebook 01 (caseiro) reusa o código da plataforma; 02 usa dlt.
# ⚠️ Requer Python 3.10+ (a plataforma roda 3.12). O strattum_core usa sintaxe
#    de tipos do 3.10+ (str | None), então o notebook caseiro NÃO roda no 3.9.
sqlalchemy>=2.0
psycopg[binary]>=3.1      # o conector caseiro usa psycopg v3 (postgresql+psycopg://)
pyarrow>=14
deltalake>=0.16
duckdb>=1.0
pyyaml>=6.0
dlt[sql_database]>=1.4
ipykernel>=6.0
# strattum_core (importado pelo conector caseiro) precisa destes:
pydantic>=2.0
pydantic-settings>=2.0
structlog>=24.0
polars>=1.0
boto3>=1.34            # criar bucket no MinIO (notebook 07)
great_expectations>=1.0  # checks de data quality na ingestão (notebook 09)
